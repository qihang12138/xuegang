<view class="vip box-sizing" wx:if="{{ id==1 }}" style="background:url('{{pageData.year_vip.image}}')no-repeat 0/100% 100%;">
  <view class="vip-tit">¥{{pageData.year_vip_money.year_money}}元立即开通</view>
  <view class="vip-text">开通{{pageData.year_vip.name}}享受最优惠的价格</view>
</view>
<commodity for="{{pageData.goods}}" vip="{{false}}" wx:if="{{id==0}}"></commodity>
<commodity for="{{pageData.goods_lot}}" vip="{{false}}" wx:elif="{{id==1}}"></commodity>
<commodity for="{{pageData.goods}}" vip="{{false}}" wx:elif="{{id==2}}"></commodity>
